# UI 重设计方案（桌面 / 手机）

> 本文是路由界面重新设计的目标蓝图与决策记录，供实现时对照。
> 最后更新：2026-08

---

## 0. 设计动机

现有 UI 的问题：

1. **按数据模型组织，而非按用户任务组织**：用户要回答"我想用这个模型，现在能用吗"，但 UI 让用户先选分组 → 再配 Vendor → 再看逻辑模型 → 展开 Key 才能看到映射与健康。
2. **入口太多，职责重叠**：发送栏有 `[⚡]`（Quick Actions）、`[≡]`（控制台）、`[🎲]`（手动路由）三个按钮，旧侧栏还有一个面板。用户不知道点哪个。
3. **健康状态藏在深处**：熔断/冷却状态需要"展开 Vendor → 展开 Key → 看模型 pill"才可见。核心信息不可达。
4. **ui.ts 是巨石**（1194 行），难以维护。
5. **手机端完全没设计**：侧栏三栏在手机上不可用。

**核心原则**：「我想用这个模型，现在能用吗？」这个问题应在 1 秒内被回答。所有其他功能是次要配置。

---

## 1. 入口策略

### 1.1 统一入口：Quick Actions 菜单顶部

不再保留独立的控制台发送栏按钮 `[≡]`。控制台入口放到 Quick Actions 下拉菜单的**最顶部**：

```
发送栏 [⚡] 按钮
  └── 点击弹出 Quick Actions 菜单
       ├── 🗂️ 路由控制台      ← 新加，置顶
       ├── ⚙️ 设置            ← 管理便捷方案
       ├── 📄 查看日志         ← 日志查看
       ├── ───────────
       ├── ⚡ DeepSeek V3     ← 已有的快捷方案
       └── ...
```

### 1.2 保留的独立按钮

- `[🎲]` 手动路由按钮：保留在发送栏，功能独立（锁定下一次路由到指定 Vendor/Key/Model），不整合进控制台。
- 旧侧栏面板按钮：保留兼容入口，但不再主动引导用户使用。

### 1.3 入口对比

| 方案 | 优点 | 缺点 |
|------|------|------|
| 独立 `[≡]` 按钮 | 一步直达 | 发送栏拥挤，按钮多 |
| **Quick Actions 顶部** ✅ | 少一个按钮，Quick Actions 已成路由中心 | 多一步点击（先点 `[⚡]` 再点"路由控制台"） |

选择 QA 顶部的原因是：控制台不是日常高频操作（日常切模型用 Quick Actions 方案就够了），多一步可以接受。

---

## 2. 桌面端：三栏浮层面板

ST 侧栏空间金贵，路由配置的复杂度不该挤在里面。改为**点击按钮弹出的浮层**，三栏并列。

### 2.1 左栏：逻辑模型仪表盘（核心视图，用户每天看的）

```
┌─────────────────────────────────────┐
│  🔍 搜索模型...                      │
│                                      │
│  🟢 DeepSeek V3      硅基流动 · 主号 │
│     3 条可用路由，最优延迟 1.2s      │
│                                      │
│  🟡 Gemini 2.5 Pro   谷歌 · 备用    │
│     2 条路由，1 条冷却中 (剩余 3m)   │
│     ⚠️ 主号模型不可用，已切备用      │
│                                      │
│  🔴 GPT-4o           无可用路由      │
│     ⚠️ 所有 Vendor 均不可用         │
│                                      │
│  ⚪ Claude 3.5       未配置          │
│     添加 Key 即可开始使用            │
└─────────────────────────────────────┘
```

- 每个逻辑模型一行，显示**综合健康状态** + 当前最优路由 + 关键诊断
- 🟢 正常 / 🟡 有降级 / 🔴 不可用 / ⚪ 未配置
- 不强迫理解 Vendor/Key/Mapping 概念即可用

**实现状态**：✅ 已实现（`dashboard.ts`）——每行副标题即最优路由摘要（`modelStatus().best` 的 Vendor · Key · 真实模型），冷却中显示剩余时长。

### 2.2 中栏：路由详情（选中逻辑模型后展开）

```
┌─────────────────────────────────────┐
│  ← 返回  DeepSeek V3                │
│                                     │
│  ── 可用路由 ──                     │
│  🟢 硅基流动 · 主号   3s 冷却       │
│  🟢 硅基流动 · 备用   权重 1.0     │
│  🟡 DeepSeek 官方 · K1 冷却中 2m   │
│     [手动恢复]                      │
│  🔴 硅基流动 · 测试号  余额不足     │
│     [手动恢复]                      │
│                                     │
│  ── 最近失败记录 ──                 │
│  10:23:45  硅基流动·主号  timeout   │
│  10:20:12  官方·K1  429 rate limit  │
└─────────────────────────────────────┘
```

- 健康状态**默认可见**（无需展开 Vendor/Key）
- 路由按健康排序；冷却/不可恢复直接显示剩余时间 + 手动恢复按钮
- 保留最近失败记录（诊断）

**实现状态**：✅ 已实现（`route-detail.ts`）。中栏底部展示**全局 200 条滑动窗口**“最近错误与结果观测”（`QuickerApiSettings.observationHistory`），失败与空回复都记录，最新在前，带时间、Vendor·Key·真实模型、分类与消息；生成结束落盘后派发事件实时刷新（桌面与手机面板）。

### 2.3 右栏：管理面板（四个 tab）

```
┌─────────────────────────────────────┐
│  [设置] [Vendor] [路由] [映射]       │
│                                      │
│  ── 当前 tab 内容 ──                 │
│                                      │
│  (设置 tab)                          │
│  路由开关 / sticky 次数 / 冷却参数   │
│                                      │
│  (Vendor tab)                        │
│  🔽 硅基流动 (custom)                │
│    ├─ 主号  ●●●●○○ 成功率 80%       │
│    ├─ 备用  ●●●●●○ 成功率 95%       │
│    └─ [+ 添加 Key]                  │
│  🔽 谷歌 (gemini)                    │
│    └─ 主力  ●●●●●● 成功率 100%     │
│  [+ 添加 Vendor]                    │
│                                      │
│  (路由 tab)                          │
│  分组切换下拉 / 刷新模型 / 导入导出   │
│                                      │
│  (映射 tab)                          │
│  从旧面板搬过来的映射管理             │
└─────────────────────────────────────┘
```

**实现状态**：
- ✅ 设置 tab（路由参数 + 分组切换下拉 + 模型管理 + 数据管理 + 映射规则/忽略，`right-route.ts` + `right-mapping.ts`）
- ✅ Vendor tab（含成功率进度条，`right-vendor.ts`）
- ✅ 旧面板 `ui.ts` 已删除，功能全部并入新控制台

> 注：原规划的 4 tab 已收敛为 3 区（设置 = 路由+映射合并；见 §8.5）。

---

## 3. 手机端：底部弹出面板（Bottom Sheet）

> ✅ 初版已实现（`console-panel-mobile.ts`）：底部 Sheet（点击上滑手柄收起），模型 tab 复用 `dashboard.ts`/`route-detail.ts`，管理 tab 复用 `right-*`。`console-panel.openConsolePanel` 在 `isMobileViewport()`（≤720px）时路由到 mobile。
> 本节为完整设计蓝图；初版做了核心（模型切换 + 路由详情 + 管理），拖拽手势、分组隐藏等未做。

手机侧栏不可行。改用**底部弹出面板** + 手势操作。

### 3.1 常态：底部一行 + 上滑手柄

```
┌──────────────────────┐
│                      │
│    (主聊天界面)       │
│                      │
│   ┌──────────────┐   │
│   │  ⚡ 路由状态  │   │  ← 轻触切换
│   └──────────────┘   │
│  ┌────────────────┐  │
│  │                │  │
│  │  底部面板上滑   │  │  ← 拖动手柄
│  └────────────────┘  │
└──────────────────────┘
```

- 底部始终显示：当前逻辑模型名 + 健康圆点（🟢🟡🔴）
- 点一下快速切换模型；上滑展开完整面板

### 3.2 展开面板（简化，隐藏高级概念）

```
┌──────────────────────┐
│ ═══  拖拽手柄  ═══   │
│                      │
│  DeepSeek V3    🟢   │  ← 当前模型
│  ┌────────────────┐  │
│  │  硅基流动·主号  │  │  ← 大按钮，显示当前路由
│  │  延迟 1.2s     │  │
│  └────────────────┘  │
│  ── 其他模型 ──      │
│  Gemini 2.5   🟡    │  ← 点一下切换
│  GPT-4o      🔴     │
│  + 添加模型          │
│  [ 路由详情 ]  [ 设置 ]│  ← 底部 tab
└──────────────────────┘
```

- **没有"分组"**（手机用户多为单分组，隐藏）
- **没有 Vendor 大表单**（加 Key 走小弹窗）
- 当前路由显示为大按钮

### 3.3 路由详情页 / 设置页

同桌面版功能，但布局改为单列堆叠，全屏高度。

---

## 4. 模型映射区（独立设置页）

> 已在 `mapping-tools.ts` 实现基础版（批量规则 / 忽略 / 一键归类）。

**原则**：映射是一次性设置任务，日常界面完全消失；但设置过程对几百个模型也要友好。

```
┌──────────────────────────────────────────┐
│  模型映射  (在设置面板里，不是主界面)      │
│                                          │
│  ┌─ 自动归类 ──────────────────────────┐ │
│  │  「一键归类所有未映射的模型」按钮     │ │
│  │  按核心名归并，5 秒做完              │ │
│  └──────────────────────────────────────┘ │
│  ── 批量规则 ──                          │
│  ┌────────────────────────────────────┐  │
│  │  /kimi\|k3/i  →  kimi-k3   [⚡][✏][🗑] │ │
│  │  （点击会出现实时预览与目标选择）    │  │
│  └────────────────────────────────────┘  │
│  [+ 添加规则]                            │
│                                          │
│  ── 已映射 (23 个) ──                   │
│  │ Vendor │ 真实模型        │ 逻辑     │ │
│  │ 硅基   │ deepseek-chat  │ DS3      │ │
│  │ 谷歌   │ gemini-2.0     │ GN2      │ │
│  │ 硅基   │ deepseek-reason│ DS4      │ │
│  │ ...（表格，分页/搜索/虚拟滚动）      │ │
│                                          │
│  ── 未映射 (3 个) ──                    │
│  ├─ xxx-embedding-v3  [忽略]  [归类]    │
│  ├─ xxx-reranker      [忽略]  [归类]    │
│  └─ test-model-foo    [忽略]  [归类]    │
│  ── 已忽略 ──                           │
│  ├─ xxx-embedding-v3/xxx-reranker（可取消）│
└──────────────────────────────────────────┘
```

**实现状态**：✅ `mapping-tools.ts` 已有基础版，但当前只在旧面板 `ui.ts` 中初始化。需要搬到新控制台右栏作为第四个 tab（"映射"）。

---

## 5. 执行计划

### Phase 1 — 入口整合 + 补齐桌面新控制台

| 步骤 | 改动 | 文件 |
|------|------|------|
| 1.1 | Quick Actions 菜单顶部加"路由控制台"条目，调 `openConsolePanel()` | `src/quick-actions/menu.ts` |
| 1.2 | 移除发送栏 `[≡]` 按钮（`consoleEntry`），不再创建 | `src/routing/manual-route-entry.ts` |
| 1.3 | 左栏仪表盘补「最优路由摘要」行 | `src/routing/ui/dashboard.ts` |
| 1.4 | 中栏路由详情补「最近失败记录」区块 | `src/routing/ui/route-detail.ts` |
| 1.5 | 右栏 Vendor tab 补成功率进度条 | `src/routing/ui/right-vendor.ts` |
| 1.6 | 右栏路由 tab 加分组的快速切换下拉 | `src/routing/ui/right-route.ts` |

### Phase 2 — 映射 tab + 旧面板瘦身

| 步骤 | 改动 | 文件 |
|------|------|------|
| 2.1 | 新建右栏"映射"tab，从旧面板搬映射区 | `src/routing/ui/right-mapping.ts`（新） |
| 2.2 | 旧面板删除已搬走的映射区代码 | `src/routing/ui.ts` |
| 2.3 | 旧面板剩余部分折叠为兼容入口 | `src/routing/ui.ts` |

### Phase 3 — 手机端

| 步骤 | 改动 | 文件 |
|------|------|------|
| 3.1 | 新建手机端底部面板编排器 | `src/routing/ui/console-panel-mobile.ts`（新） |
| 3.2 | 响应式检测，桌面/手机切换 | `console-panel.ts`（新增 `isMobile` 判断） |
| 3.3 | 复用已有渲染函数（左栏/中栏/右栏） | 各渲染模块 |

---

## 6. 模块拆分状态（当前）

### 新控制台模块（`src/routing/ui/`）

| 文件 | 职责 | 行数 | 状态 |
|------|------|------|------|
| `console-panel.ts` | 编排器（状态 + refresh + open/close） | 174 | ✅ |
| `console-panel-styles.ts` | CSS 注入 | 1 export | ✅ |
| `console-helpers.ts` | 共享辅助函数 | 91 | ✅ |
| `dashboard.ts` | 左栏仪表盘渲染 | 41 | ✅ 缺最优路由摘要 |
| `route-detail.ts` | 中栏路由详情渲染 | 63 | ✅ 缺失败记录 |
| `right-settings.ts` | 右栏设置 tab | 18 | ✅ |
| `right-vendor.ts` | 右栏 Vendor 管理 tab | 330 | ✅ 缺成功率进度条 |
| `right-route.ts` | 右栏路由 tab | 375 | ✅ 缺分组切换 |
| `right-mapping.ts` | 右栏映射 tab | — | ❌ 待创建 |
| `mapping-tools.ts` | 映射规则/忽略/一键归类 | — | ✅ 旧面板在用 |

### 旧面板（需退役）

| 文件 | 职责 | 行数 | 状态 |
|------|------|------|------|
| `src/routing/ui.ts` | 旧路由面板 | 1194 | ⏳ 待拆 |
| `src/routing/ui/vendor-panel.ts` | 旧面板 Vendor 渲染 | 446 | ⏳ 与新 `right-vendor.ts` 重叠 |

### 入口模块

| 文件 | 职责 | 状态 |
|------|------|------|
| `src/routing/manual-route-entry.ts` | 发送栏按钮注入 | ✅ 保留 `[🎲]`，移除 `[≡]` |
| `src/quick-actions/menu.ts` | Quick Actions 菜单 | ✅ 加"路由控制台"条目 |

---

## 7. 刻意不做

- ❌ 全屏拖拽/图形化映射（过度设计）
- ❌ 实时 websocket 连接测试（违反约束）
- ❌ SVG 拓扑图（好看但无用）
- ❌ 手机端 Vendor 大表单（加 Key 走小弹窗即可）
- ❌ 手机端分组切换（默认单分组，隐藏）
---

## 8. 当前 UI 待办 / 缺口（2026-08 讨论记录）

> 控制台（三栏浮层）已落地，以下是待补的设计缺口与已落地项，按优先级排。

### 8.1 Vendor 卡片重设计（已完成）

- 头部重新布局：名称横排主行（flex:1），格式/成功率抽到 `.csl-vendor-subrow` 副行 ✅
- 健康**正常模型聚合折叠**：正常收进「🔽 N 个正常」折叠，异常(冷却/不可恢复)直接显示 ✅
- 冷却模型**手动恢复改弹窗确认**（`Popup.show.confirm`）✅

### 8.2 Vendor Key 管理（已完成）

- 展开区底部「＋ 添加 Key」按钮（`normalizeGroupEntry` 追加到当前分组该 Vendor）✅
- （删除 Key 未加：保留在展开区改 key 值即可，可后续补）

### 8.3 禁用 Vendor / Key 排列表底部（已完成）

Vendor 列表与 Key 列表按「启用在前、禁用在后」（各自名称/label 序）排序。✅

### 8.4 仪表盘当前逻辑模型置顶（已完成）

dashboard 排序：当前分组选中的逻辑模型置顶 + `is-current` 高亮。✅

### 8.5 右栏聚合（已完成）

- 「刷新全部模型」移到 Vendor tab 顶部（消除 route/vendor 重复）✅
- **映射 tab 并入设置 tab**：右栏 4→3 tab（设置/Vendor/路由），设置 = 路由参数 + 映射规则/忽略 ✅
- **逻辑模型附加参数编辑入口移到左栏**：dashboard 每行 ⚙ 按钮（`logical-params-editor.ts` 共享）✅

### 8.6 控件复用（已完成）

- `routing/ui/controls.ts` 的 `showEditorDialog`：统一编辑弹窗（标题/保存/取消/extraActions），6 个弹窗迁移 ✅
- quick-actions（方案管理）保持独立视觉体系，不强行接入（用户决定）

### 8.7 连接状态：启动置空地址占位（已完成，2026-08）

**问题**：拦截模式不写 `oai_settings`，导致 ST「自动连接上次服务器」可能连到上次路由/手动配置的 vendor。

**方案**：`initQuickerApi` 调 `ensureEmptyConnectionPlaceholder()`：把连接置为 **custom + 空 URL + 空 model** + `saveSettingsDebounced()`，并 `setOnlineStatus('Valid')`。
- 持久化空地址 → 下次启动 `isValidUrl('')` false，RA_autoconnect 无 target 可连（治本）
- source=custom 同时切断 RA_autoconnect 的 deepseek 分支
- 只改 oai_settings + 下拉显示，**不 trigger('change')**（避免 reconnect / /v1/models）
- 真实连接由拦截模式（`patchGenerateData`）生成时接管

### 8.8 优先级（用户指定）

1. 控件级复用（已完成）
2. 文档更新（本文档）
3. Vendor 卡片 / Key 增删 / 禁用排序 / pill 交互（已完成）
4. 启动空占位（已完成）

### 8.9 待办（下一步）

- **手机端底部面板**：初版已实现（`console-panel-mobile.ts`）；待做：拖拽手势、分组隐藏、Vendor 大表单改弹窗
- **日志增强**：已实现 debug 时 `installFetchLogging`（记 /secrets/status/generate/models 请求）

---

## 9. 视觉与交互系统 v2（2026-08 第二次重做）

> 用户决策：统一深色卡片风（自包含设计令牌）；三级按钮体系；控制台全量重做。
> 实现：`console-panel-styles.ts` 全量重写（设计令牌 + 按钮体系），各渲染器替换按钮类名。

### 9.1 设计令牌（挂在 `.csl-overlay` / `.qam-overlay` 根上）

| 令牌 | 值 | 用途 |
|---|---|---|
| `--csl-bg` | `#16181d` | 面板底 |
| `--csl-card` / `--csl-card-hover` | `#1e2128` / `#23262e` | 卡片/悬停面 |
| `--csl-border` / `--csl-border-strong` | `rgba(255,255,255,.06)` / `.12` | 描边两级 |
| `--csl-text` / `--csl-text-dim` / `--csl-text-faint` | `#e8eaee` / `#9aa0aa` / `#6b7078` | 文字三级 |
| `--csl-accent` | `#5b9bd5` | 主色/选中 |
| `--csl-ok` / `--csl-warn` / `--csl-danger` | `#7ecf8a` / `#e0c07e` / `#e08a8a` | 健康三色 |
| `--csl-radius` | `10px` | 圆角基准 |

### 9.2 三级按钮体系（`.csl-btn`）

| 类 | 用途 | 形态 |
|---|---|---|
| `--primary` | 每个区域唯一主操作 | accent 实底白字 |
| `--secondary` | 常规操作 | card 底 + strong 描边 |
| `--icon` | 高频小操作 | ghost，tooltip 说明 |
| `--danger` | 破坏性操作 | 红描边，hover 红实底 |
| `[disabled]` / `.is-loading` | 异步进行中 | 半透明 + 图标旋转 |

规则：每个卡片/区块最多一个 primary；异步按钮点击即进入 loading 态（禁点+转圈），完成恢复。
旧 `menu_button` 在控制台内全部替换；quick-actions 菜单保持独立视觉（用户既定决策）。

### 9.3 交互修复清单（随本次重做落地）

1. 导入配置：解析后弹摘要确认（合入数量 + 是否覆盖设置），不再选完文件直接覆盖。
2. 批量刷新：按钮实时显示「刷新中 i/N」；单个失败不再双份 toast（quiet 参数抑制内部报错）。
3. `cslNumber` 支持 min/max 钳制并回写显示；阈值类字段带取值提示。
4. 「重置模型数据」确认文案修正（实际不自动重新拉取）。
5. 「从已拉取模型创建」执行前弹确认（该操作含 prune 回收副作用）。
6. 拉取模型路径的 secretId 回退漏洞修复（同 B9：写 secret 失败即取消拉取，不带空 id 打 status）。

### 9.4 刻意不做（v2 补充）

7. 路由详情每行新增「禁用/启用」按钮：手动禁用单个真实模型（`entry.disabledModels`，选路排除 + reconcile 清扫 + Vendor 健康胶囊显示「手动禁用」态）。
8. 映射规则目标选择器接入 select2 可搜索；映射 tab 按钮统一三级体系。
9. 移除「导出模型列表」；日志导出统一走 QA 菜单（控制台诊断区删除）。
- ❌ 亮色主题适配（ST 使用场景以深色皮肤为主，令牌集中后续可加）
